import { Spectral } from 'next/font/google'

export const type_second = Spectral({
  weight: ['700'],
  style: ['normal'],
  subsets: ['latin'],
  display: 'swap',
  variable: '--type-second-spectral',
})
